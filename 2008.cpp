#include <bits/stdc++.h>

using namespace std;

typedef vector<int> vi;
typedef vector<vi> vvi;

int main()
{
    ios::sync_with_stdio(false);

    int D, P, R, B;

    while(cin >> D >> P >> R >> B)
    {
        vi DSP(D), PPP(P);

        for(int i = 0; i < D; i++) {
            cin >> DSP[i];
        }

        for(int i = 0; i < P; i++) {
            cin >> PPP[i];
        }

        vvi

        while(R--)
        {
            int X, Y;

            cin >> X >> Y;
        }
    }

    return 0;
}



