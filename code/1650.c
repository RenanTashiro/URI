#include <bits/stdc++.h>

int main()
{
    int N, M;

    while( cin >> N >> M && N != 0 )
    {
        int ans = 0;

        int row = M - 8;
        int column = N - 8;

        cout << row << " " << column << "\n";
    }

    return 0;
}
