#include <bits/stdc++.h>

using namespace std;

int main()
{
    int N;
    list<int> time;

    scanf( "%d", &N );

    for( int i = 0; i < N; i++ ) {
        int Ns;
        cin >> Ns;
        time.push( Ns );
    }

    return 0;
}
