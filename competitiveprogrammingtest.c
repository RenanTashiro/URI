#include <stdio.h>
#include <math.h>

int main()
{
    int i;

    scanf( "%d", &i );

    while( i-- ){
        ///Do something
    }

    ///while (scanf("%d %d", &a, &b), (a || b))

    return 0;
}
