#include <iostream>
#include <stdio.h>
using namespace std;

int main()
{
    string tag, number, text;

    while(cin >> tag >> number){

    getline(cin,text);

    cout << tag << number << text;

    }
}
